apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: qatra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: qatra/notification-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres:5432/qatra
            - name: SPRING_DATASOURCE_USERNAME
              value: qatra
            - name: SPRING_DATASOURCE_PASSWORD
              value: qatra
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: qatra
spec:
  selector:
    app: notification-service
  ports:
    - port: 8082
      targetPort: 8082